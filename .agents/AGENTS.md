
## User Preferences and Build Rules
- Do NOT run `flutter build apk` or any other build commands after making changes unless explicitly requested.
- ALWAYS run `flutter analyze` after every modification to check for errors.
