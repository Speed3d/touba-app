# 🔍 التحليل الشامل والمرجع التأسيسي لمشروع «طوبة» ⚽

> **هذا المستند هو المرجع التقني الأساسي والكامل لمشروع طوبة.**
> يصف **الحالة الفعلية للكود كما هو على الأرض** (لا كما هو مأمول في الخطط)، ويرصد كل خطأ وملاحظة وثغرة، ويضع خطة عمل مرتبة بالأولوية للبناء عليه.
>
> 📌 **مستند حيّ** — يُحدَّث مع كل تغيير جوهري. راجع «سجل إصدارات المستند» في النهاية.

---

## 0. معلومات المستند

| الحقل | القيمة |
|---|---|
| النسخة | 1.0 |
| تاريخ التحليل | 15 حزيران/يونيو 2026 |
| المُحلِّل | Claude (مراجعة كاملة للكود والخطط) |
| نطاق التحليل | كامل المستودع: `lib/`، `functions/`، قواعد الأمان، الفهارس، إعداد Firebase، هوية أندرويد/iOS، الخطط في `docs/` |
| الطريقة | قراءة كل ملف مصدري + `flutter analyze` + فحص الإعدادات |
| كيفية التحديث | عدّل القسم المعني، وزِد سطراً في «سجل إصدارات المستند»، وحدّث «مصفوفة الحالة السريعة» |

### مفتاح درجات الخطورة
- 🔴 **حرج** — يكسر وظيفة أساسية أو يمثّل ثغرة أمنية/سلامة بيانات. يجب الحل قبل أي إطلاق.
- 🟠 **مهم** — يسبب سلوكاً خاطئاً أو دَيناً تقنياً كبيراً. يُحل في المرحلة الحالية.
- 🟡 **ملاحظة** — تحسين جودة/نظافة كود/تناسق. يُجدول لاحقاً.

---

## 1. الملخص التنفيذي (الحقيقة بصراحة)

«طوبة» هو تطبيق Flutter لمنصة كرة قدم شعبية عراقية، **مبني فوق هيكل تطبيق سابق هو «حرفي العراق» (craftsman_iraq)** عبر إعادة استخدام نفس المستودع. هذا القرار وفّر بنية تحتية جاهزة (ثيم، مصادقة، إعدادات بناء)، لكنه ترك **طبقات كاملة من التطبيق القديم لم تُنظَّف بعد**، وهي مصدر معظم المخاطر الحالية.

**ما يعمل فعلاً اليوم:**
- ✅ المصادقة بالبريد/كلمة المرور (دخول، تسجيل لدورين لاعب/كابتن، استعادة كلمة المرور).
- ✅ الملف الشخصي وتعديله ورفع الصورة/اختيار أفاتار جاهز.
- ✅ وحدة الفرق بشكل **أوّلي**: إنشاء فريق، قائمة الفرق، صفحة تفاصيل الفريق، إرسال طلب انضمام.
- ✅ نظام ثيم (فاتح/داكن) وتعريب أساسي والتطبيق **يُترجم بنجاح** (لا أخطاء بناء قاتلة).

**ما لا يعمل / غير مكتمل (والخطط تظنه قادماً أو منجزاً):**
- 🔴 **البطولات والمباريات والترتيب**: مجرّد **شاشات بيانات وهمية ثابتة (mockups)**؛ المستودعات الخاصة بها موجودة لكنها **غير مربوطة بالتطبيق إطلاقاً**.
- 🔴 **قواعد أمان Firestore و Storage والفهارس**: ما زالت **100% من تطبيق حرفي القديم** — لا توجد أي قاعدة للفرق/اللاعبين/البطولات/المباريات. النتيجة: عمليات كتابة هذه المجموعات وكذلك **رفع شعار الفريق ستُرفض** افتراضياً.
- 🔴 **الدالة السحابية** `onMatchResultConfirmed` مكتوبة بصياغة **الجيل الأول (v1) القديم** بينما الحزمة المثبتة `firebase-functions ^7` — **لن تُنشر/تعمل** كما هي.
- 🔴 **المشروع يستخدم مشروع Firebase الخاص بحرفي القديم** (`craftsman-iraq-v2`) — خلط بيانات طوبة مع بيانات حرفي الإنتاجية.
- 🟠 **هوية التطبيق غير موحّدة**: اسم الحزمة، اسم العرض، معرّف iOS كلها مختلطة بين القديم والجديد.
- 🟠 **ثغرة سلامة إحصائيات**: قواعد المستخدمين الحالية تسمح للاعب **بتعديل أهدافه وتقييمه بنفسه** — وهو بالضبط الخطر الذي تحذّر منه الخطة.

**الخلاصة:** الأساس (Auth + Profile + بذرة الفرق) سليم ويصلح للبناء عليه، لكن **«المرحلة الأولى من التنظيف وفك الارتباط»** في الخطة التنفيذية **لم تكتمل**، و**نواة المنتج (MVP) الحقيقية — البطولات والمباريات والترتيب — لم تُبنَ بعد**؛ الموجود منها واجهات شكلية فقط.

### ★ مبدأ حاكم: ترشيد الاستهلاك (Cost & Performance First)
قرار صريح من المالك: **كل شيء يجب أن يكون قليل الاستهلاك** (قراءات/كتابات Firestore، تخزين، حزمة بيانات الجوال). يُطبَّق هذا المبدأ على كل قرار تقني لاحق:
- **الصور:** ضغط قبل الرفع + **تخزين مؤقت (cache-first)** عبر `cached_network_image` (مثبّت أصلاً) → الصورة تُحمَّل مرة وتُقرأ من الكاش، وتُحدَّث مع الوقت فقط عند تغيّر الرابط.
- **الإحصائيات:** **تجميع مسبق (Pre-aggregation)** في حقول جاهزة على مستندات الفريق/اللاعب/البطولة، تُحدَّث داخل الدالة السحابية عند النتيجة → القراءة فورية ورخيصة بدل حساب لحظي مكلف.
- **القوائم:** **ترقيم صفحات (pagination)** و`limit` على كل استعلام، ومنع قراءة مجموعات كاملة.
- **التزامن:** استخدام `snapshots()` فقط حيث يلزم التحديث الحيّ (مباراة جارية)، و`get()` لمرة واحدة لبقية الشاشات.
- **الفهارس:** فهرسة مركّبة دقيقة لتجنّب استعلامات مكلفة/مرفوضة.

---

## 2. هوية المشروع والحالة العامة

| العنصر | القيمة الفعلية في الكود | المتوقّع حسب الخطة | الحالة |
|---|---|---|---|
| اسم حزمة pubspec | `popular_football` | `popular_football` | ✅ مطابق |
| الإصدار | `1.0.8+30` | — | موروث من حرفي (رقم مرتفع لتطبيق بذرة) 🟡 |
| اسم العرض (Android label) | `طوبة` | `طوبة` | ✅ |
| `strings.xml` → `app_name` | `حرفي العراق` | `طوبة` | 🟠 قديم |
| Android `namespace` | `com.craftsmaniraq.craftsman_iraq` | `com.iraq.popular_football` | 🟠 قديم |
| Android `applicationId` | `com.speed3d.touba` | `com.iraq.popular_football` | 🟠 ثالث مختلف! |
| مسار `MainActivity.kt` | `com/craftsmaniraq/craftsman_iraq/` | الجديد | 🟠 قديم |
| iOS bundle (codemagic) | `com.craftsmaniraq.craftsmanIraq` | الجديد | 🟠 قديم |
| مشروع Firebase | `craftsman-iraq-v2` | مشروع جديد مستقل | 🔴 يُعاد استخدام مشروع حرفي |
| Facebook App ID (strings.xml) | بقايا حرفي (`1481873260000375`) | يُحذف | 🟡 |
| عنوان `MaterialApp` | `طوبة` | `طوبة` | ✅ |

> **استنتاج:** هناك **ثلاثة معرّفات حزمة مختلفة** متناثرة (`craftsmaniraq`, `speed3d.touba`, والمطلوب `iraq.popular_football`). يجب حسم معرّف واحد نهائي وتوحيده عبر كل الملفات قبل أول نشر، لأن تغييره بعد النشر على المتاجر **مستحيل** (يعني تطبيقاً جديداً).

---

## 3. خريطة المعمارية الفعلية

المعمارية المتبعة هي **Clean-ish Architecture بطبقات** مع **Bloc/Cubit** لإدارة الحالة (وهو الخيار الذي حسمه المالك في القرار رقم 4).

```
lib/
├── main.dart                 ← يهيّئ Firebase + MultiBlocProvider (Theme, Locale, Auth, Team)
├── app/theme/                ← نظام ثيم ضخم موروث من حرفي (≈2400 سطر، 10 ملفات)
├── l10n/                     ← تعريب (ar/en) — 12 مفتاح فقط، مستخدم جزئياً
├── core/error/               ← AuthException فقط
├── data/
│   ├── models/               ← user, team, player, tournament, match, location
│   ├── repositories/         ← user, team, tournament, match, player, config, base
│   ├── services/auth/        ← AuthService (Firebase Auth)
│   └── seeds/                ← iraq_coordinates_seed (غير مستخدم)
└── presentation/
    ├── cubits/               ← auth/, team/, theme_cubit, locale_cubit
    └── screens/              ← auth/, main/, teams/, tournaments/, matches/,
                                 profile/, admin/
```

### تدفق التطبيق عند الإقلاع
```
main() → Firebase.initializeApp() (بدون firebase_options — يعتمد على google-services.json)
       → AuthWrapper (يستمع AuthCubit)
            ├── AuthInitial/Loading → سبينر
            ├── AuthAuthenticated / AuthVisitor → MainScreen
            └── غير ذلك → LoginScreen

MainScreen (BottomNav 4 تبويبات):
   [0] TournamentsScreen  ← ⚠️ بيانات وهمية ثابتة
   [1] TeamsScreen        ← ✅ مربوط بـ TeamCubit
   [2] MatchesScreen      ← ⚠️ بيانات وهمية ثابتة
   [3] ProfileScreen      ← ✅ مربوط بـ AuthCubit
```

### ما هو مربوط فعلياً مقابل ما هو معلّق
| المكوّن | مربوط في `main.dart`/الواجهة؟ | ملاحظة |
|---|---|---|
| `AuthCubit` + `AuthService` + `UserRepository` | ✅ | يعمل |
| `TeamCubit` + `TeamRepository` | ✅ | يعمل |
| `ThemeCubit` / `LocaleCubit` | ✅ | يعمل (بلا حفظ تفضيل) |
| `TournamentRepository` | ❌ غير مربوط | لا يوجد Cubit ولا استدعاء |
| `MatchRepository` | ❌ غير مربوط | — |
| `PlayerRepository` + `PlayerModel` | ❌ غير مربوط | **كود ميت بالكامل** |
| `ConfigRepository` | ❌ غير مربوط | — |
| `BaseRepository` | ❌ لا يرث منه أحد | **كود ميت** |
| `AdminDashboard` / `ManageLocationsScreen` | ❌ لا منفذ تنقّل إليهما | غير قابلة للوصول من الواجهة |
| `iraq_coordinates_seed.dart` | ❌ | بذرة موروثة غير مستخدمة |

---

## 4. تحليل الفجوة: الخطة مقابل المنفّذ

مقارنة بين ما تدّعيه `docs/football_platform_master_plan.md` / `docs/original_plan.txt` وبين الواقع.

| البند في الخطة | ادعاء الخطة | الواقع في الكود | الفجوة |
|---|---|---|---|
| المصادقة والأدوار | ✅ مكتمل 100% | ✅ يعمل (بريد فقط) | تطابق، لكن لا تحقق بريد/هاتف ولا تسجيل اجتماعي رغم وجود الحِزَم |
| الملف الشخصي | ✅ مكتمل 100% | ✅ يعمل | تطابق |
| **التنظيف وفك الارتباط** (المرحلة 1 بالخطة التنفيذية) | مفترض أولوية أولى | ❌ غير مكتمل | قواعد/فهارس/Storage/Firebase ما زالت لحرفي |
| وحدة الفرق | ⏳ «قادمة» | 🟡 مبنية جزئياً (إنشاء/قائمة/تفاصيل/طلب انضمام) | الكود **متقدّم على** Master Plan هنا، لكن ينقصه: قبول/رفض الطلبات، إدارة الفريق، إحصائيات الفريق الحيّة، حقول الفريق الكاملة |
| البطولات | ⏳ «قادمة» | ❌ Mockup فقط | لا إنشاء بطولة، لا اختيار فرق، لا توليد جدول |
| المباريات والنتائج | ⏳ «قادمة» | ❌ Mockup + دالة معطّلة | العصب الأساسي للمنصة غير موجود فعلياً |
| الترتيب التلقائي | ⏳ «قادمة» | ❌ | يعتمد على الدالة السحابية المعطّلة |
| المعالجة الذرّية للنتائج | مذكورة كأخطر تفصيل تقني | ⚠️ مكتوبة لكن لا تعمل (v1/v7) وناقصة | لا reverse-then-apply، لا idempotency، لا إحصائيات لاعبين |
| القرارات المفتوحة (القسم 14) | حُسم معظمها | بعضها مطبّق (Bloc، لهجة عراقية)، بعضها لا | تفصيل في القسم 14 أدناه |

> **ملاحظة منهجية:** يوجد **مستندان للخطة** (`master_plan` المختصر + `original_plan` المفصّل) + `implementation_plan`. هناك **تعارضات بينها وبين الكود** (مثل: المباريات subcollection في الخطة مقابل top-level في الكود، ونقاط التصنيف). يجب اعتماد **مصدر حقيقة واحد** وتحديثه.

---

## 5. تحليل وحدة بوحدة

### 5.1 المصادقة (Auth) — ✅ جيدة
- `AuthService` يغلّف Firebase Auth ويترجم رموز الأخطاء للعربية (جيد).
- `AuthCubit` يستمع لـ `authStateChanges` ويجلب مستند المستخدم تلقائياً.
- التسجيل يتحقق من تفرّد رقم الهاتف قبل الإنشاء.
- 🟠 **حِزم معطّلة الاستخدام**: `google_sign_in`, `sign_in_with_apple` مثبّتة لكن لا تطبيق لها في `AuthService` رغم أن الخطة لا تطلبها في MVP.
- 🟡 لا تحقق من البريد الإلكتروني (Email Verification) ولا تحقق هاتف.
- 🟡 إن وُجد حساب Auth بلا مستند Firestore، الحالة تصبح `AuthUnauthenticated` بصمت (قد يحبس المستخدم خارج التطبيق دون رسالة).

### 5.2 الملف الشخصي (Profile) — ✅ جيدة
- `ProfileScreen` يعرض الإحصائيات (مباريات/أهداف/تقييم) والتفاصيل الفنية من `UserModel`.
- `EditProfileScreen` يدعم رفع صورة/أفاتار جاهز.
- 🟡 الإحصائيات تُقرأ من `UserModel` مباشرة — وهذه نقطة الثغرة الأمنية (انظر 11.1): لا شيء يمنع المستخدم من تعديلها.

### 5.3 الفرق (Teams) — 🟡 أوّلية تعمل جزئياً
- إنشاء فريق (للكابتن فقط — يُتحقق من `role == 'captain'` في الواجهة)، رفع شعار، قائمة فرق مع بحث، صفحة تفاصيل، طلب انضمام.
- 🔴 **رفع الشعار سيُرفض**: `TeamRepository.uploadTeamLogo` يكتب إلى `teams/logos/{id}.jpg` بينما `storage.rules` لا تحوي مسار `teams/` وتنتهي بقاعدة رفض شاملة.
- 🔴 **كتابة مستند الفريق ستُرفض**: لا قاعدة `match /teams/...` في `firestore.rules`.
- 🟠 **طلبات الانضمام طريق مسدود**: `requestToJoin` يكتب في مجموعة `join_requests`، لكن **لا توجد أي شاشة للكابتن لعرض/قبول/رفض** الطلبات، ولا قاعدة أمان للمجموعة.
- 🟠 «إدارة الفريق» زر يعرض «ستتوفر قريباً» فقط (`team_details_screen.dart:163`).
- 🟠 حقول الفريق ناقصة جداً مقابل الخطة (لا منطقة/حيّ، ألوان، سنة تأسيس، هاتف مسؤول، وصف، نقاط تصنيف ضمن الموديل...).
- 🐞 **خطأ منطقي** (انظر 10): `team_details_screen.dart:55` `players.firstWhere(..., orElse: () => players.first)` يرمي استثناءً إذا كانت `players` فارغة.

### 5.4 البطولات (Tournaments) — ❌ واجهة وهمية
- `TournamentsScreen` يعرض **بطاقات ثابتة مكتوبة يدوياً** («بطولة بغداد الكبرى»، «دوري رمضان السداسي»...).
- `TournamentRepository` فيه `createTournament` + `getAllTournaments` فقط، **غير مستدعى من أي مكان**.
- لا توليد جدول، لا اختيار فرق، لا حقول `pointsRule`/`tiebreakers`/شعار في `TournamentModel`.

### 5.5 المباريات (Matches) — ❌ واجهة وهمية + خلفية معطّلة
- `MatchesScreen` بيانات ثابتة (نجوم المنصور 2-1 شباب الكرادة...).
- `MatchRepository.updateMatchResult` يضع `resultConfirmed: true` **من العميل مباشرة** (يُفترض أن يكون حصراً للمنظّم — والقاعدة غائبة أصلاً).
- يعتمد كلياً على الدالة السحابية المعطّلة (القسم 7).

### 5.6 لوحة الإدارة (Admin) — ❌ غير موصولة
- `AdminDashboard` + `ManageLocationsScreen` موجودتان لكن **لا منفذ تنقّل** إليهما من أي شاشة، ولا تحقق من دور admin لإظهارهما.

---

## 6. تحليل طبقة البيانات والنماذج

### 6.1 🔴 ازدواجية تمثيل «اللاعب» (أخطر تناقض معماري)
يوجد **تمثيلان متنافسان للاعب**:
1. **`UserModel`** (مجموعة `users`): يحمل `goals, assists, rating, matchesPlayed, position, preferredFoot...`. **هذا هو المستخدَم فعلياً** — الفرق تخزّن `playersIds` تشير إلى مستندات `users`، و`TeamCubit.fetchTeamDetails` يجلب اللاعبين عبر `UserRepository.getPlayersByIds`.
2. **`PlayerModel`** (مجموعة `players`) + `PlayerRepository`: كيان منفصل بحقول `careerStats, currentTeamId, shirtNumber...`. **غير مستخدم إطلاقاً (كود ميت).**

الخطة (`original_plan` 6.2) تتبنّى النموذج الثاني صراحةً («كل لاعب كيان مستقل له سجل دائم… مهم جداً لنظام الانتقالات»)، بينما الكود نفّذ الأول.

> **قرار مطلوب حسمه فوراً:** هل اللاعب = حساب مستخدم (`users`)؟ أم سجل مستقل (`players`)؟ هذا القرار يحكم نظام الانتقالات وبطاقات اللاعبين والإحصائيات لاحقاً. القرار رقم 1 في الخطة فضّل «سجل يديره الفريق» — وهو يميل للنموذج الثاني، **لكن الكود ذهب للأول**. لا يمكن البناء قبل توحيدهما.

### 6.2 تناقضات النماذج الأخرى
| النموذج | ملاحظة | خطورة |
|---|---|---|
| `TeamModel` | لا يحوي `ratingPoints` رغم أن الدالة السحابية تكتبه على مستند الفريق → يُفقد عند كل `toJson`/تحديث | 🟠 |
| `TeamModel` | ينقصه: area, colors, foundedYear, managerPhone, badges, description, photos | 🟡 |
| `TeamStats` | يحوي `points` لكن الدالة السحابية **لا تحسب نقاط الفريق العامة** (تحسب نقاط الترتيب داخل البطولة فقط) | 🟠 |
| `TournamentModel` | ينقصه `pointsRule`, `tiebreakers`, `logoUrl`, عدد الجولات | 🟡 |
| `MatchModel` | top-level مع `homeScore/awayScore` مسطّحة (يخالف بنية الخطة `score:{home,away}` و subcollection) | 🟠 تعارض خطة/كود |
| `models.dart` | يصدّر `user_model` فقط (barrel ناقص) + تعليق يذكر `craftsman_iraq` | 🟡 |

### 6.3 المستودعات (Repositories)
- 🟠 **لا يرث أي مستودع من `BaseRepository`** رغم وجوده ووجود منطق معالجة أخطاء/إعادة محاولة فيه → `BaseRepository` كود ميت، ومعالجة الأخطاء غير موحّدة.
- 🟡 رسائل الأخطاء العربية تُلتقط وتُلفّ بـ `Exception('...')` فتضيع تفاصيل الخطأ الأصلي (يصعّب التشخيص).

---

## 7. تحليل الخلفية (Cloud Functions)

ملف `functions/index.js` يحوي دالة واحدة `onMatchResultConfirmed`. التقييم:

| # | المشكلة | الخطورة |
|---|---|---|
| 1 | **صياغة الجيل الأول (v1)**: `functions.firestore.document(...).onUpdate` بينما `package.json` يثبّت `firebase-functions: ^7.0.0` حيث أُزيل هذا الـ API من الجذر. يجب `require("firebase-functions/v1")` أو الترحيل لـ v2 (`onDocumentUpdated`). **الدالة لن تُنشر/تعمل كما هي.** | 🔴 |
| 2 | تستمع على `matches/{matchId}` (top-level) — متوافقة مع كود التطبيق، لكنها **تخالف الخطة** (subcollection داخل البطولة). يجب توحيد المصدر. | 🟠 |
| 3 | **لا تحدّث إحصائيات اللاعبين** (أهداف/تمريرات/بطاقات) رغم أن الخطة تعتبره جوهر «المعالجة الذرّية». | 🟠 |
| 4 | **لا «تراجع ثم تطبيق» (Reverse-then-Apply)** عند تعديل نتيجة مؤكّدة → تعديل النتيجة سيُضاعف الإحصائيات. | 🔴 سلامة بيانات |
| 5 | **لا Idempotency** (بصمة تطبيق) → أي إعادة تشغيل تحتسب النتيجة مرتين. | 🟠 |
| 6 | نقاط التصنيف **+30/−15/+5** في الكود مقابل **+20/+5/−10** في الخطة. تعارض غير محسوم. | 🟡 |
| 7 | لا تحديد **منطقة (region)** — الخطة توصي بـ `europe-west1` لتقليل الكُمون. | 🟡 |
| 8 | اعتماديات بقايا حرفي: `jsonwebtoken`, `jwks-rsa` بلا استخدام. | 🟡 |
| 9 | `predeploy` يشغّل `eslint .` — قد يفشل النشر إن وُجدت مخالفات lint. | 🟡 |
| 10 | تحدّث `stats` كاملاً عبر قراءة-تعديل بدل `FieldValue.increment` (أأمن من سباقات التزامن، لكنه داخل transaction فمقبول). | 🟡 |

---

## 8. تحليل الأمان (قواعد Firestore و Storage) — 🔴 أخطر منطقة

### 8.1 قواعد Firestore (`firestore.rules`) — 100% قديمة
الملف بالكامل يخص حرفي: `users, chats, service_requests, quotations, ratings, activation_codes, job_posts, cvs, …`. **لا توجد ولا قاعدة واحدة** لـ:
`teams`, `players`, `tournaments`, `matches`, `join_requests`.

**الأثر المباشر:** بسبب مبدأ Firestore «ما لا يُسمح به صراحةً مرفوض»، **كل عمليات الكتابة على مجموعات طوبة مرفوضة افتراضياً**. (إنشاء فريق، إنشاء بطولة، تحديث نتيجة، طلب انضمام — كلها ستفشل عند تفعيل القواعد على المشروع الحقيقي).

> ملاحظة: قد يبدو أن الفرق «تُنشأ» أثناء التطوير لأن القاعدة المنشورة فعلياً على مشروع `craftsman-iraq-v2` ربما تختلف عن هذا الملف، أو لأن وضع الاختبار مفتوح. لكن **اعتماداً على هذا الملف، الإنتاج سيكسر**.

### 8.2 🔴 ثغرة سلامة الإحصائيات في قاعدة `users` الحالية
القاعدة الحالية:
```
allow update: if isOwner(userId) || isAdmin() || (... profileViews ...)
```
بما أن `UserModel.toJson()` يكتب `goals, assists, rating, matchesPlayed`، و`UserRepository.updateUser` يرسل المستند كاملاً، و**صاحب الحساب مسموح له بتعديل مستنده** → **أي لاعب يستطيع تضخيم أهدافه وتقييمه يدوياً**. هذا تحديداً «خطر الثقة في النتائج» الذي تحذّر منه الخطة (3.2). يجب فصل حقول الإحصائيات لتُكتب من **Cloud Functions فقط**.

### 8.3 قواعد Storage (`storage.rules`) — 100% قديمة
مسارات حرفي فقط (`users/{uid}/profile`, `works`, `id`, `requests`, `chats`, `banners`...). **لا مسار `teams/`** → رفع شعار الفريق مرفوض. صور الملف الشخصي تعمل لأنها على مسار `users/{uid}/profile` المسموح — لكن **التطبيق يرفع للبروفايل على `users/profile_images/{uid}.jpg`** (مسار مختلف غير مذكور في القواعد!) → **رفع صورة البروفايل أيضاً سيُرفض في الإنتاج**.

### 8.4 الفهارس (`firestore.indexes.json`) — 100% قديمة
كل الفهارس لمجموعات حرفي. عند بناء استعلامات البطولات/المباريات/الترتيب المركّبة ستظهر أخطاء «requires an index». يجب تخطيط فهارس طوبة مبكراً (الخطة 7.5 تنصح بذلك).

### 8.5 إعدادات أمان غير مفعّلة
- `firebase_app_check` مثبّت لكن **لا يُهيّأ** في `main.dart`.
- `firebase_crashlytics` و`firebase_analytics` مثبّتان لكن **لا يُهيّآن** (لا التقاط أخطاء/تحليلات).

---

## 9. تحليل البنية التحتية وإعداد Firebase

| العنصر | الحالة | خطورة |
|---|---|---|
| `google-services.json` (Android) | موجود، يشير لـ `craftsman-iraq-v2` | 🔴 مشروع قديم |
| `GoogleService-Info.plist` (iOS) | **غير موجود** | 🔴 iOS سيتعطّل عند `Firebase.initializeApp()` |
| `lib/firebase_options.dart` | **غير موجود** | 🟠 يعتمد على الملفات الأصلية فقط؛ لا دعم متعدد المنصات |
| `.firebaserc` | `default = craftsman-iraq-v2` + أهداف hosting لحرفي | 🔴 |
| `firebase.json` | hosting/firestore/storage/functions لحرفي | 🟠 |
| `codemagic.yaml` | iOS فقط، bundle قديم، بريد المالك مضبوط | 🟡 |
| runtime الدوال | Node 24 + firebase-functions v7 (تعارض مع كود v1) | 🔴 |

> **قرار جوهري مطلوب:** إنشاء **مشروع Firebase جديد مستقل لطوبة** (موصى به بشدة لعزل البيانات والفوترة والقواعد) أم الاستمرار على `craftsman-iraq-v2`. الاستمرار يعني أن قواعد/فهارس حرفي **يجب ألا تُمسح** بل يُضاف إليها، وأن بيانات طوبة ستختلط بإنتاج حرفي.

---

## 10. الأخطاء البرمجية الملموسة (مع المواقع)

| # | الموقع | الوصف | خطورة |
|---|---|---|---|
| B1 | `lib/presentation/screens/teams/team_details_screen.dart:55` | `players.firstWhere(..., orElse: () => players.first)` يرمي `StateError` إذا كانت القائمة فارغة (مثلاً مستند الكابتن مفقود) | 🟠 |
| B2 | `team_details_screen.dart:249` | `_buildPlayerCard(context, var player)` يستخدم نوعاً ديناميكياً بدل `UserModel` → فقدان أمان النوع | 🟡 |
| B3 | `lib/presentation/screens/teams/teams_screen.dart:152` | `${team.playersIds.length + 1}` يفترض أن الكابتن دائماً خارج `playersIds`؛ قد يُحتسب مرتين | 🟡 |
| B4 | `lib/presentation/cubits/theme_cubit.dart` | الثيم لا يُحفظ (لا `shared_preferences`)؛ يُعاد للفاتح عند كل تشغيل، و`LocaleCubit` غالباً مثله | 🟡 |
| B5 | `lib/data/repositories/team_repository.dart:5` | استيراد `user_model.dart` غير مستخدم | 🟡 |
| B6 | `lib/presentation/screens/profile/edit_profile_screen.dart:7` | استيراد غير مستخدم + `use_build_context_synchronously` في السطر 162 | 🟡 |
| B7 | `lib/presentation/screens/profile/ready_images_selector_screen.dart` | عدة استيرادات غير مستخدمة (`dart:io`, `typed_data`, `image_picker`, `path_provider`) | 🟡 |
| B8 | `tournaments_screen.dart:48,57` / `tooba_card.dart:32` / `admin_dashboard.dart:69` | `withOpacity` مهجور → استخدم `.withValues(alpha:)` | 🟡 |
| B9 | `create_team_screen.dart:207` / `edit_profile_screen.dart:266,286` | `DropdownButtonFormField.value` مهجور → `initialValue` | 🟡 |
| B10 | `integration_test/app_test.dart` | الاختبار فارغ (الكود كله مُعلّق) | 🟡 |

**نتيجة `flutter analyze`:** 58 ملاحظة إجمالاً، **لا أخطاء بناء قاتلة**. لكن **غالبية الـ 58 صادرة من مجلد `craftsman_backup/`** الذي يُحلَّل خطأً ضمن المشروع (انظر 12).

---

## 11. الثغرات الأمنية ومخاطر سلامة البيانات

| # | الثغرة | الأثر | الحل المقترح |
|---|---|---|---|
| V1 | لاعب يعدّل إحصائياته بنفسه (8.2) | تزوير الأهداف/التقييم → انهيار مصداقية المنصة | فصل حقول الإحصائيات؛ كتابتها من Cloud Functions فقط؛ قاعدة `update` تمنع تعديل هذه الحقول من العميل |
| V2 | غياب قواعد `teams/tournaments/matches` (8.1) | إمّا كسر وظيفي (رفض) أو — لو فُتحت بإهمال — كتابة حرّة | كتابة قواعد كاملة: تأكيد النتيجة لمنظّم البطولة فقط، تعديل الفريق للكابتن فقط |
| V3 | `resultConfirmed` يُضبط من العميل (`MatchRepository`) | تلاعب بالنتائج | حصره بالمنظّم عبر القواعد + التحديث عبر Cloud Function |
| V4 | App Check غير مفعّل | استغلال الواجهات الخلفية من خارج التطبيق | تفعيل App Check وتهيئته في `main.dart` |
| V5 | خلط بيانات طوبة مع إنتاج حرفي (نفس مشروع Firebase) | تلوّث بيانات/خصوصية/فوترة | مشروع Firebase مستقل لطوبة |
| V6 | بقايا مفاتيح Facebook في `strings.xml` | تسريب معرّفات تطبيق طرف ثالث | إزالتها |

> لا توجد أسرار حرجة مكشوفة في الكود المصدري عدا معرّفات Facebook العامة (وهي معرّفات عميل، أقل حساسية، لكنها بقايا يجب تنظيفها).

---

## 12. الكود الميت والاعتماديات الزائدة

### 12.1 كود ميت يجب حذفه أو ربطه
- `lib/data/models/player_model.dart` + `lib/data/repositories/player_repository.dart` (غير مستخدمين — انظر 6.1).
- `lib/data/repositories/base_repository.dart` (لا يرث منه أحد).
- `lib/data/repositories/config_repository.dart`, `tournament_repository.dart`, `match_repository.dart` (غير مربوطة — تُربط لاحقاً أو تُحذف مؤقتاً).
- `lib/data/seeds/iraq_coordinates_seed.dart` (بذرة موروثة).

### 12.2 🟡 `craftsman_backup/` — نسخة مرجعية مقصودة من التطبيق القديم
مجلد بحجم ضخم (يحوي `node_modules`, `build`, `.git`, كامل `lib` القديم...) موجود داخل جذر المشروع (غير متتبّع في git).

> **توضيح المالك (مقصود):** هذا المجلد **محفوظ عمداً كمرجع** للاستعانة وتذكّر كيف بُنيت أشياء معيّنة في حرفي، **وسيُحذف نهائياً بعد اكتمال مشروع طوبة**. فهو ليس فوضى يجب إزالتها الآن.

التحفّظ التقني الوحيد المتبقّي: المجلد **يُلوّث `flutter analyze`** (معظم الـ 58 ملاحظة صادرة منه) وقد يبطئ أدوات الفهرسة. **الحل الموصى به دون حذفه:** استثناؤه من المحلّل عبر `analysis_options.yaml`:
```yaml
analyzer:
  exclude:
    - craftsman_backup/**
```
وكذلك التأكد من أنه **لا يدخل في حزمة البناء الإنتاجية** (مجلدات خارج `lib/` و`assets/` المعلنة لا تُحزَّم تلقائياً، لذا الأثر على حجم التطبيق معدوم). يبقى المجلد مرجعاً حتى اكتمال طوبة ثم يُحذف.

### 12.3 اعتماديات pubspec مثبّتة بلا استخدام (موروثة من حرفي)
أمثلة ترفع حجم التطبيق وزمن البناء دون فائدة حالية:
`flutter_map`, `latlong2`, `flutter_map_marker_cluster`, `geolocator`, `mobile_scanner`, `excel`, `csv`, `qr_flutter`, `photo_view`, `carousel_slider`, `fl_chart`, `local_auth`, `flutter_secure_storage`, `flutter_form_builder`, `hive`, `flutter_local_notifications`, `firebase_remote_config`, `flutter_rating_bar`, `shimmer`, `device_info_plus`, `crypto`, `package_info_plus`...

> توصية: مراجعة كل اعتمادية — الإبقاء على ما تحتاجه نواة طوبة فقط، وحذف الباقي بعد إزالة استخداماته (إن وُجدت). يُؤجَّل ما قد يُستخدم قريباً (مثل `share_plus` لبطاقات اللاعبين، `cached_network_image`).

---

## 13. الجودة والاختبارات والتعريب

- 🟠 **لا توجد اختبارات وحدة**: لا مجلد `test/`، و`integration_test` فارغ — رغم تثبيت `firebase_auth_mocks`, `fake_cloud_firestore`, `mockito`, `mocktail` (كلها بلا استخدام). يُنصح ببدء اختبارات للـ Cubits والمستودعات مبكراً.
- 🟡 **التعريب جزئي**: `app_ar.arb`/`app_en.arb` فيهما 12 مفتاحاً فقط، ومعظم الشاشات **تكتب النصوص العربية مباشرة (hardcoded)** بدل استخدام `AppLocalizations`. القرار كان «لهجة عراقية» — وبعض النصوص بالفعل بلهجة («هلا بيك»، «سجل فريقك») لكن غير متسقة.
- 🟡 **نظام الثيم ضخم وموروث**: ≈2400 سطر في `lib/app/theme/` (نظامان «classic/glass»، ألوان بنفسجي/teal في التعليقات بينما اللون الفعلي أزرق `#1877F2`). أكبر من حاجة المشروع الحالية؛ تعليقات الألوان مضلّلة.
- 🟡 **تعارض ألوان الهوية**: اللون الأساسي أزرق `#1877F2`، بينما خلفية أيقونة التطبيق `#0D9488` (teal) في `pubspec`. يجب توحيد لوحة الألوان.
- 🟡 `coverage/lcov.info` بقايا قديمة.

---

## 14. القرارات المفتوحة (حالتها الحالية)

من `original_plan.txt` القسم 14، مع ما طُبّق فعلاً:

| # | القرار | ما اختاره المالك | الحالة في الكود |
|---|---|---|---|
| 1 | حساب اللاعب أم سجل يديره الفريق؟ | سجل يديره الفريق (مبدئياً) | ⚠️ الكود نفّذ «حساب مستخدم» — **تعارض، يحتاج حسم** |
| 2 | انسحاب فريق | خسارة افتراضية 3-0 | ❌ غير مطبّق |
| 3 | نموذج التصنيف | يُقرَّر لاحقاً | الكود يستخدم +30/−15/+5 (مؤقت) |
| 4 | إدارة الحالة | Bloc | ✅ مطبّق (Cubit) |
| 5 | اللهجة | عراقية | 🟡 جزئياً |
| 6 | احتساب الودّيات | لا تُحتسب | ❌ لا نظام مباريات بعد |
| 7 | الوقت المتاح | 20–30 ساعة/أسبوع | — تخطيطي |
| 8 | البطولة الأولى | جاهزة فعلاً | — تشغيلي |

**قرارات برزت من التحليل — ✅ حُسمت (15 حزيران 2026):**
- **C1 ✅:** معرّف الحزمة النهائي = **`com.speed3d.touba`**. (Android App ID: `1:868610876965:android:4f7300e3b93e01725f81cc`). يجب توحيده عبر namespace/مجلد Kotlin/iOS bundle/`app_name`.
- **C2 ✅:** **مشروع Firebase جديد مستقل لطوبة** — Project ID: **`touba-3ds`**، Project number: **`868610876965`**. يُعاد توليد `google-services.json` + `GoogleService-Info.plist` + `firebase_options.dart` على هذا المشروع.
- **C3 ✅:** **مجموعة top-level `matches`** (الأنسب للاستهلاك ولتبويب المباريات حسب التاريخ عبر البطولات). يُحدَّث مسار الدالة السحابية والخطة ليطابقا ذلك.
- **C4 ✅ (نموذج هجين):** **سجل `players` مستقل يديره الكابتن** (المركز, الرقم، الحالة، الفريق) + **الإحصائيات تُكتب من Cloud Functions حصراً** (أهداف/صناعة/تقييم تُجمَّع من أحداث المباريات لا تُكتب يدوياً) + **ربط اختياري بحساب `users`** عبر `claimedByUid`؛ عند الربط يملك اللاعب تعديل حقوله الشخصية فقط (الصورة، الاسم المعروض، القدم المفضّلة، البريد، الهاتف). **تفاصيل توزيع الملكية ومنطق «المطالبة بالحساب» قيد النقاش التفصيلي.**
- **C5 ✅:** المنصات = **أندرويد و iOS معاً** من البداية. يتطلّب: `GoogleService-Info.plist` على `touba-3ds`, حساب Apple Developer، إعداد توقيع iOS، وتحديث bundle id في codemagic إلى `com.speed3d.touba`.

---

## 15. خطة العمل المرتّبة بالأولوية (Roadmap للإصلاح والبناء)

### المرحلة 0 — حسم القرارات وتثبيت الأساس (قبل أي كود جديد)
1. حسم القرارات C1–C4 أعلاه (هي تحكم كل ما يليها).
2. اعتماد **مستند خطة واحد** كمصدر حقيقة (دمج الثلاثة)، وتحديث هذا التحليل عند التغيير.

### المرحلة 1 — إكمال «التنظيف وفك الارتباط» (🔴 أولوية قصوى)
3. توحيد الهوية: `applicationId`/`namespace`/مسار Kotlin/iOS bundle/`app_name`/إزالة Facebook.
4. حسم Firebase: إنشاء مشروع طوبة الجديد (موصى به) + `google-services.json` + `GoogleService-Info.plist` + توليد `firebase_options.dart` عبر FlutterFire CLI.
5. **كتابة `firestore.rules` و`storage.rules` و`firestore.indexes.json` لطوبة** من الصفر (فرق/لاعبون/بطولات/مباريات/طلبات انضمام)، مع:
   - تأكيد النتيجة لمنظّم البطولة فقط.
   - تعديل الفريق للكابتن فقط.
   - منع العميل من كتابة حقول الإحصائيات (V1).
   - مسار Storage لشعارات الفرق + تصحيح مسار صور البروفايل.
6. استثناء `craftsman_backup/` من المحلّل (`analysis_options.yaml`) مع **إبقائه كمرجع** (قرار المالك)؛ يُحذف نهائياً بعد اكتمال طوبة.
7. تنظيف اعتماديات `pubspec.yaml` غير المستخدمة.

### المرحلة 2 — إصلاح الخلفية والمعالجة الذرّية (🔴)
8. ترحيل `onMatchResultConfirmed` إلى صياغة متوافقة مع firebase-functions v7 (v2 `onDocumentUpdated` أو `/v1`).
9. إضافة: تحديث إحصائيات اللاعبين + Reverse-then-Apply + Idempotency + ضبط region.
10. توحيد نقاط التصنيف مع الخطة، وإضافة `ratingPoints` للنموذج.

### المرحلة 3 — إكمال نواة MVP الحقيقية (🟠)
11. إكمال وحدة الفرق: شاشة إدارة طلبات الانضمام (قبول/رفض)، حقول الفريق الكاملة، نقل الملكية.
12. بناء وحدة البطولات فعلياً: `TournamentCubit`، إنشاء بطولة، اختيار فرق، **توليد الجدول** (Round-Robin)، وربط `TournamentsScreen` بالبيانات الحقيقية.
13. بناء وحدة المباريات والترتيب: `MatchCubit`، شاشة إدخال نتيجة (للمنظّم)، ربط `MatchesScreen` والترتيب الحيّ.
14. توحيد نموذج اللاعب (إزالة الازدواجية حسب C4).

### المرحلة 4 — الصقل والجودة (🟡)
15. تفعيل Crashlytics/Analytics/App Check في `main.dart`.
16. حفظ تفضيل الثيم/اللغة، إصلاح أخطاء B1–B10، إزالة الكود الميت.
17. اختبارات وحدة للـ Cubits والمستودعات + اختبار تكامل حقيقي.
18. توحيد التعريب (نقل النصوص إلى ARB) ولوحة الألوان.

---

## 16. مصفوفة الحالة السريعة

| الوحدة/الطبقة | الحالة | الجاهزية للإنتاج |
|---|---|---|
| المصادقة | ✅ تعمل | 80% |
| الملف الشخصي | ✅ تعمل | 75% (ثغرة V1) |
| الفرق | 🟡 جزئية | 40% |
| البطولات | ❌ Mockup | 5% |
| المباريات والنتائج | ❌ Mockup + خلفية معطّلة | 5% |
| الترتيب | ❌ | 5% |
| لوحة الإدارة | ❌ غير موصولة | 5% |
| قواعد أمان Firestore | 🔴 قديمة | 0% (لطوبة) |
| قواعد Storage | 🔴 قديمة | 0% (لطوبة) |
| الفهارس | 🔴 قديمة | 0% (لطوبة) |
| Cloud Functions | 🔴 معطّلة | 20% |
| هوية أندرويد | 🟠 مختلطة | 50% |
| هوية/Firebase iOS | 🔴 ناقصة | 10% |
| الاختبارات | 🔴 غائبة | 0% |
| **الإجمالي التقديري للـ MVP** | — | **≈ 25%** |

---

## 17. سجل إصدارات المستند

| النسخة | التاريخ | التغييرات |
|---|---|---|
| 1.0 | 15 حزيران/يونيو 2026 | الإصدار الأول — تحليل شامل كامل للكود والخطط والإعدادات، رصد الأخطاء والثغرات، وخطة إصلاح مرتّبة. |
| 1.1 | 15 حزيران/يونيو 2026 | تصحيح بند `craftsman_backup/`: هو نسخة مرجعية **مقصودة** يحتفظ بها المالك وتُحذف بعد اكتمال طوبة (خُفِّضت الخطورة لـ 🟡 وتغيّرت التوصية إلى استثناء من المحلّل بدل الحذف). |
| 1.2 | 15 حزيران/يونيو 2026 | حسم القرارات C1–C4 (معرّف `com.speed3d.touba`، مشروع Firebase جديد `touba-3ds`، مباريات top-level، نموذج لاعب هجين). إضافة «مبدأ ترشيد الاستهلاك» الحاكم. |
| 1.3 | 15 حزيران/يونيو 2026 | حسم C5: المنصات أندرويد + iOS معاً. بدء النقاش التفصيلي لنموذج اللاعب (C4). |

---

> **كيفية استخدام هذا المستند:** اعتمده كنقطة بداية لكل جلسة عمل. عند إنجاز بند من القسم 15، حدّث «مصفوفة الحالة السريعة» (16) وأضف سطراً في «سجل الإصدارات» (17). عند تغيّر قرار، حدّث القسمين 14 و4. أبقِ هذا الملف **مصدر الحقيقة الوحيد** لحالة المشروع.
