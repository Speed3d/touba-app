# 📊 تقرير طوبة — الحالة الشاملة

> **تاريخ التقرير:** 19 آب/أغسطس 2026
> **أُعدّ بـ:** فحص مباشر للكود والملفات والتاريخ — لا نقلاً عن مستندات قديمة
> **الغرض:** صورة كاملة موثّقة — تُقرأ على أي جهاز (ويندوز أو ماك) لمعرفة أين وصلنا.
> **المرجع الهندسي:** [`تصميم_الأساس.md`](تصميم_الأساس.md) · **سجل التنفيذ:** [`تحليل_المشروع_الشامل.md`](تحليل_المشروع_الشامل.md) القسم 19.

---

## 1) أرقام المشروع (مُحقَّقة مباشرة)

| البند | القيمة |
|---|---|
| ملفات Dart في `lib/` | **~148** |
| الشاشات | **60** (في 19 مجلد) |
| النماذج (Models) | **21** |
| المستودعات (Repositories) | **13** |
| الكيوبتس (Cubits) | **12** |
| الودجات المشتركة (`widgets/core/`) | **13** |
| الخدمات (Services) | **6** |
| Cloud Functions منشورة | **25 — مؤكَّدة على `touba-3ds`** |
| مفاتيح الترجمة | **1353** مفتاح × 3 لغات |
| `flutter analyze lib` | ✅ **No issues found** (19 آب 2026) |
| `flutter test` | ✅ **17/17** (19 آب 2026) |
| ESLint (functions) | ✅ 0 (آخر فحص 22 تموز 2026) |
| الإصدار | `1.0.1+2` |
| مستودع Git | `github.com/Speed3d/touba-app.git` |
| آخر commit | `c8df067` — 22 تموز 2026 |

---

## 2) هيكل المشروع الكامل

```
lib/
├── main.dart                          # نقطة البداية + DI (10 Repositories + 5 Cubits) + Firebase init
├── firebase_options.dart              # إعداد Firebase (android + iOS فقط — web مؤجّل)
├── app/
│   ├── router/tooba_route.dart        # انتقال موحّد (Fade + Scale 0.96→1.0)
│   └── theme/ (11 ملف)               # نظام تصميم كامل: app_colors / app_color_system /
│                                      # app_text_styles / app_theme / button_theme /
│                                      # card_theme / design_tokens / input_theme /
│                                      # navigation_theme / theme.dart
├── core/
│   ├── constants/iraq_regions.dart    # 18 محافظة + مناطق (252 نص عربي — تحتاج nameEn/nameKu)
│   ├── error/auth_exceptions.dart     # استثناءات مخصّصة
│   └── utils/
│       ├── formations.dart            # خوارزمية مراكز اللاعبين
│       ├── image_helper.dart          # رفع الصور
│       └── tooba_snack_bar.dart       # SnackBar موحّد
├── data/
│   ├── models/ (21 نموذج)            # activation_code · banner · challenge · chat_message ·
│   │                                  # chat · city · district · join_request · location ·
│   │                                  # match · news · notification · player · referee_application ·
│   │                                  # release_request · report · team · tournament_lineup ·
│   │                                  # tournament · user + models.dart
│   ├── repositories/ (13 مستودع)     # base · challenge · chat · config · home · location ·
│   │                                  # match · player · subscription · team · tournament_lineup ·
│   │                                  # tournament · user
│   ├── services/ (6 خدمات)           # auth/auth_service · fixtures_pdf_service ·
│   │                                  # fixtures_service · functions_service ·
│   │                                  # notification_service · preferences_service
│   └── seeds/
│       └── iraq_coordinates_seed.dart # إحداثيات GPS للـ18 محافظة
├── l10n/ (8 ملفات)
│   ├── app_ar.arb                     # القالب (76 KB) — 1353 مفتاح
│   ├── app_en.arb                     # الإنجليزية (63 KB)
│   ├── app_ku.arb                     # الكردية السوراني (90 KB — ترجمة حقيقية)
│   ├── app_localizations*.dart (4)    # مُولَّد تلقائياً بـ flutter gen-l10n
│   └── ku_material_localizations.dart # 🆕 delegates مخصّصة للكردية (RTL + Material)
└── presentation/
    ├── cubits/ (12)
    │   ├── auth/ (auth_cubit + auth_state)
    │   ├── team/ (team_cubit + team_state + team_manage_cubit + team_manage_state)
    │   ├── tournament/ (tournament_cubit + tournament_state)
    │   ├── notifications/ (notifications_cubit + notifications_state)
    │   ├── locale_cubit.dart
    │   └── theme_cubit.dart
    ├── screens/ (60 شاشة في 19 مجلد)
    │   ├── admin/ (13)                # dashboard · banners · news · subscriptions · challenges ·
    │   │                              # disputes · reports · sections · tournaments · referee_apps ·
    │   │                              # manage_locations · manage_districts · manage_roles
    │   ├── auth/ (4)                  # auth_wrapper · login · register · forgot_password
    │   ├── challenges/ (2)            # challenges · create_challenge
    │   ├── chat/ (2)                  # chat · chats_list
    │   ├── home/ (4)                  # home · news · news_detail · banner_details
    │   ├── legal/ (3)                 # legal_scaffold · privacy_policy · terms_conditions
    │   ├── main/ (1)                  # main_screen
    │   ├── matches/ (4)               # match_detail · matches · referee_matches · tournament_matches
    │   ├── notifications/ (1)         # notifications
    │   ├── onboarding/ (1)            # onboarding
    │   ├── players/ (1)               # player_detail
    │   ├── profile/ (3)               # profile · player_profile_edit · ready_images_selector
    │   ├── referee/ (1)               # referee_profile
    │   ├── reports/ (1)               # submit_report
    │   ├── settings/ (1)              # settings
    │   ├── splash/ (1)                # splash
    │   ├── subscription/ (2)          # subscription · subscription_locked_sheet
    │   ├── teams/ (7)                 # teams · create_team · team_details · team_management ·
    │   │                              # team_formation_edit · team_gallery_edit · add_player
    │   └── tournaments/ (8)           # tournaments · create_tournament · tournament_details ·
    │                                  # tournament_matches · enter_result · tournament_lineup ·
    │                                  # tournament_lineups_review · tournament_standings ·
    │                                  # tournament_rules_editor
    └── widgets/core/ (13 ودجت)
        ├── bracket_view.dart          # شجرة الإقصائي
        ├── decorated_background.dart  # خلفية تصميم v2
        ├── formation_share_sheet.dart # مشاركة التشكيلة
        ├── live_match_timer.dart      # مؤقّت المباراة الحيّة (خادمي)
        ├── pitch_formation_view.dart  # الملعب التفاعلي
        ├── section_header.dart        # عنوان قسم
        ├── standings_view.dart        # جدول الترتيب
        ├── tooba_bottom_nav.dart      # شريط تنقل سفلي عائم (4 أزرار)
        ├── tooba_card.dart            # بطاقة موحّدة
        ├── tooba_empty_state.dart     # حالة فارغة
        ├── tooba_match_card.dart      # بطاقة مباراة
        ├── tooba_shimmer.dart         # تحميل shimmer
        └── tooba_team_avatar.dart     # صورة الفريق الدائرية
```

---

## 3) ما أُنجز — تفصيل كامل

### 3.1 الوجبات 1–4 (النواة + MVP) — منشورة
- ✅ المصادقة: تسجيل (لاعب/كابتن/حكم) · دخول · نسيان كلمة المرور · زائر
- ✅ البروفايل: عرض + تعديل كامل (بيانات + صورة)
- ✅ الفرق: إنشاء · تفاصيل · إدارة · انضمام بطلب · انتقال · طلب خروج (بتأكيد كلمة المرور)
- ✅ البطولات: إنشاء (6/8/11 لاعب) · توليد جدول · ترتيب حيّ · تتويج البطل
- ✅ المباريات: جدولة + نتيجة موسّعة + أحداث بأيقونات
- ✅ اللاعبون: بطاقة + `careerStats` من Cloud Functions
- ✅ الإعلانات والأخبار: CRUD + إعجاب/مشاركة
- ✅ البلاغات + لوحة إدارة (13 شاشة)
- ✅ الإشعارات: FCM + داخلية
- ✅ Splash + Onboarding + صفحات قانونية

### 3.2 الوجبة الخامسة ✅ — منشورة
- ✅ مباريات التحدي + المحادثات (كابتن↔كابتن)
- ✅ نظام الاشتراكات بأكواد تفعيل (الكابتن يدفع فقط، تفعيل يدوي من الأدمن)
- ✅ إغلاق تعارض الانضمام (أول قابل يفوز)
- ✅ قفل الميزات بالاشتراك + `subscription_locked_sheet`

### 3.3 الوجبة السادسة ✅ — منشورة
- ✅ **(أ) إصلاحات عاجلة:** قواعد settings/chats + إصلاح `setState` في `matches_screen`
- ✅ **(ب) مباراة حيّة:** `LiveMatchTimer` خادمي (`matchStartedAt`) + أزرار بدأ/انتهى/شوط تالٍ + جدولة مرنة (يومي ثابت + عدد مباريات/يوم) + تقرير PDF بخط Cairo RTL
- ✅ **(ج) تشكيلات البطولة:** `TournamentLineupModel` + `tournament_lineups/{tId}_{teamId}` + تعيين تفاعلي على الملعب + مراجعة المنظّم (قبول/رفض) + تغيير خطة لكل مباراة من الكابتن
- ✅ **(د) تنقل:** شريط سفلي عائم `ToobaBottomNav` (الرئيسية/الفريق/المحادثات/الإعدادات) + الرئيسية كروت
- ✅ **(هـ) ملفات:** ملف اللاعب الكامل (قدم/طول/وزن/عمر/نبذة/5 صور) + معرض صور الفريق + اسم كابتن كل فريق عند إنشاء البطولة

### 3.4 الوجبة السابعة ✅ — منشورة (26 حزيران)
- ✅ **شجرة خروج المغلوب:** `BracketView` + CF `generateKnockout` + ترقية الفائز تلقائياً + Bye لأي عدد فرق
- ✅ **نظام المجموعات:** `qualifiersPerGroup` (1/2/3) · `assignGroups` · ترتيب واعٍ بالمجموعات · توليد إقصائي من المجموعات (idempotent)
- ✅ **لوحة مباراة حيّة:** سكور لحظي + إنهاء شوط + شوط تالٍ + تأكيد نهائي + حسم التعادل بالركلات (`penaltyHome/penaltyAway/decidedBy`)
- ✅ **الحكّام موسّع:** `onUserCreated` (CF trigger) + `rateReferee` (callable) + `RefereeProfileScreen` + قيد التقييم (مرة واحدة، لا تقييم النفس)
- ✅ **`TournamentStandingsScreen`** + `StandingsView` + تبويب «الفرق المشاركة» + `remindLineup`
- ✅ **العدّاد:** 25 Cloud Function منشورة

### 3.5 البيئة الموحّدة ✅ (22 تموز)
- ✅ `الحالة_الحالية.md` كذاكرة مشتركة بين الجهازين
- ✅ `craftsman_backup` صار submodule نظامي عبر `.gitmodules`
- ✅ VS Code: `launch.json` (منفذ 5000 ويندوز / 5001 ماك) · `settings.json` · `tasks.json` · `extensions.json`
- ✅ `دليل_الإعداد_على_الماك.md` مع قائمة تحقّق 9 بنود
- ✅ `.gitattributes` لتوحيد نهايات الأسطر (CRLF/LF)

### 3.6 التعريب — مُنجز جزئياً (19 آب 2026)
- ✅ `ku_material_localizations.dart` (ملف جديد): delegates RTL للكردية
- ✅ `manage_locations_screen`: معرَّبة بالكامل
- ✅ 6 ودجات معرَّبة بـ~20 مفتاح جديد
- ✅ `admin_subscriptions_screen`: 5 مفاتيح جديدة
- ✅ تعديل `gradle.properties` (ذاكرة Kotlin/Gradle)
- **التغطية الإجمالية: 58/60 شاشة**

---

## 4) 🔴 ما تبقّى — بانتظار قرار سنان

### الأولوية 1 — إكمال التعريب (~15% متبقّي)

| # | المهمة | الحجم | ملاحظة |
|---|---|---|---|
| أ | رسائل الأخطاء (repositories/services/cubits) | ~90 نص | بلا `context` — **يحتاج قرار نهج أولاً** |
| ب | بيانات المحافظات/المناطق | 252 نص | `iraq_regions.dart` — `nameEn`/`nameKu` |
| ج | `fixtures_pdf_service` | ~15 نص | قرار: تُترك عربية أو تُعرَّب |

**قرار النهج المطلوب لرسائل الأخطاء:**
1. رسالة عامة مترجمة في الواجهة (الأسهل)
2. أكواد أخطاء تُترجَم في الواجهة (الأنظف)
3. قبول العربية كاحتياطي (المقبول — الأخطاء نادراً ما تظهر)

> ⚠️ **أخطاء المصادقة (login/register/reset) مرئية للمستخدم وتحتاج ترجمة فعلية!**

### الأولوية 2 — إكمال تصميم v2
- تطبيق التصميم الجديد على بقية الشاشات (حالياً: `decorated_background` + نظام ألوان فقط)
- مرجع: [`تصميم_واجهة_طوبة_v2.md`](تصميم_واجهة_طوبة_v2.md)

### الأولوية 3 — ترقية أدوات أندرويد (معلّقة متعمّداً)
| المكوّن | الحالي | المطلوب |
|---|---|---|
| Gradle | `8.11.1` | `≥8.14` |
| AGP | `8.9.1` | `≥8.11.1` |
| Kotlin | `2.1.0` | `≥2.2.20` + هجرة `android.builtInKotlin=false` |
> البناء يعمل حالياً — تُنفَّذ قبل الإطلاق.

### الأولوية 4 — ما قبل الإطلاق
| # | المهمة | الحالة |
|---|---|---|
| أ | اختبار على هاتف حقيقي | لم يجرِ بعد الوجبة 7 |
| ب | تجهيز المتجر (لقطات + وصف + أيقونة) | لم يبدأ |
| ج | استضافة سياسة الخصوصية | لم تبدأ (مطلوبة للمتجر) |

---

## 5) نسبة الإنجاز الفعلية

| المحور | النسبة | ملاحظة |
|---|---|---|
| وظائف المنتج (MVP + الوجبات 1–7) | **~99%** | كل الميزات المخطّط لها مُنفَّذة |
| البنية الخلفية (قواعد/فهارس/25 CF) | **100%** | منشورة ومؤكَّدة |
| التعريب متعدّد اللغات | **~85%** | 58/60 شاشة + ~90 رسالة خطأ متبقية |
| التصميم v2 | **~30%** | نظام ألوان + خلفية فقط — يحتاج تمديداً |
| الاختبارات | **~20%** | 17 اختبار — تغطية محدودة |
| **جاهزية الإطلاق** | **~88%** | يحتاج: ترقية أدوات + اختبار هاتف + متجر |

---

## 6) معلّقات ومشاكل مؤكَّدة

| البند | الخطورة | التفاصيل |
|---|---|---|
| `craftsman_backup` submodule | 🟡 | يشير لـ worktree قديم غير موجود — خطأ في `git status`. إصلاحه: `git submodule deinit` ثم إعادة الإضافة |
| الويب | 🟡 | `firebase_options.dart` بلا فرع `web` — مؤجّل بقرار المالك |
| أدوات أندرويد | 🟡 | Gradle/AGP/Kotlin قديمة — Flutter يحذّر من إسقاط الدعم |
| ملفات Firebase على الماك | 🟡 | تُنقل يدوياً (google-services.json + GoogleService-Info.plist) |
| رسائل أخطاء المصادقة | 🟡 | تظهر للمستخدم بالعربية فقط |

---

## 7) الثوابت التي لا تتغيّر

| البند | القيمة |
|---|---|
| معرّف الحزمة | `com.speed3d.touba` |
| مشروع Firebase | `touba-3ds` (`868610876965`) |
| منطقة الدوال | `europe-west1` |
| Cloud Functions | **v2 فقط** |
| المباريات | مجموعة **top-level** `matches` |
| نموذج اللاعب | هجين: `players` + ربط `users` عبر `claimedByUid` |
| جدار المصداقية | `stats`/`careerStats`/`standings`/`ratingPoints`/`resultConfirmed`/`statsApplied` — **من CF حصراً** |
| `craftsman_backup/` | **مرجع للقراءة فقط** |
