# 📍 الحالة الحالية — طوبة

> 🔴 **هذا الملف يُقرأ أولاً في كل جلسة، على أي جهاز.** هو الذاكرة المشتركة بين ويندوز والماك.
> يُحدَّث **قبل كل commit**. لا تعتمد على ذاكرة محليّة — هي لا تنتقل بين الأجهزة.

| | |
|---|---|
| **آخر تحديث** | 20 آب/أغسطس 2026 |
| **حُدِّث من** | 💻 ويندوز |
| **آخر عمل منجز** | ✅ ترقية أدوات أندرويد بالكامل (Gradle 8.14 + AGP 8.11.1 + Kotlin 2.1.10 + Desugaring 2.1.4) + تصميم v2 بنسبة 100% + التعريب الشامل 100% + نجاح التحليل 0 أخطاء |

---

## 1) ما هو المشروع

**«طوبة» (Touba)** — تطبيق Flutter + Firebase لكرة القدم الشعبية في العراق.
المالك: **سنان**. لهجة الواجهة: **عراقية**. الحالة: **جاهزية إطلاق ~98%**.

---

## 2) الثوابت التقنية — لا تتغيّر دون إذن صريح

| البند | القيمة |
|---|---|
| معرّف الحزمة | `com.speed3d.touba` |
| مشروع Firebase | `touba-3ds` (`868610876965`) |
| منطقة الدوال | `europe-west1` |
| Cloud Functions | **v2 فقط** — v1 ممنوع |
| المباريات | مجموعة **top-level** `matches` |
| نموذج اللاعب | هجين: `players` (يديره الكابتن) + ربط `users` عبر `claimedByUid` |
| إدارة الحالة | **Bloc/Cubit** فقط |
| الإصدار | `1.0.1+2` |
| Gradle | `8.14` |
| AGP | `8.11.1` |
| Kotlin | `2.1.10` |
| المستودع | `github.com/Speed3d/touba-app.git` |

### 🔒 جدار المصداقية — لا يُكتب من العميل إطلاقاً
`stats` · `careerStats` · `standings` · `ratingPoints` · `resultConfirmed` · `statsApplied` · `appliedSnapshot` · `role` · `adminPermissions` · `winnerTeamId/Name` — **من Cloud Functions حصراً.**

### ⛔ ممنوعات حرجة
- لا نشر على مشروع Firebase غير `touba-3ds`.
- لا تمثيل ثانٍ للاعب على `users`.
- لا فصحى رسمية في الواجهة — عراقية.
- لا رفع أسرار: `touba_release.jks` · `key.properties` · `local.properties`.

---

## 3) أين وصلنا

### ✅ منجز ومنشور — الوجبات 1–7 + التعريب 100% + تصميم v2 100% + ترقية أدوات أندرويد 100%
- **المصادقة والبروفايل** (تسجيل/دخول/نسيت/زائر/تعديل ملف)
- **الفرق** (إنشاء/تفاصيل/إدارة/تشكيلة/انضمام/انتقال/طلب خروج/معرض صور)
- **البطولات** (دوري/مجموعات/خروج مغلوب بشجرة + Bye + توليد جدول + ترتيب حيّ + تتويج البطل)
- **المباريات** (جدولة مرنة + مباراة حيّة بمؤقّت خادمي + أحداث بأيقونات + حسم التعادل)
- **اللاعبون** (بطاقة + تعديل ملف: قدم/طول/وزن/عمر/نبذة/5 صور + `careerStats` من CF)
- **الحكّام** (تسجيل كحكم + صفحة عامة + تقييم عبر CF `rateReferee`)
- **الإعلانات والأخبار** (CRUD كامل + إعجاب/مشاركة)
- **البلاغات** (إرسال + لوحة أدمن)
- **لوحة إدارة** (13 شاشة: أدوار/اشتراكات/بنرات/أخبار/تحديات/بلاغات/مناطق/مقاطعات/توريات)
- **الإشعارات** (FCM + داخلية + `NotificationService`)
- **الاشتراكات** بأكواد تفعيل (الكابتن يدفع فقط)
- **التحدّي والمحادثات** (كابتن↔كابتن)
- **التشكيلات** (تعيين تفاعلي على الملعب + مراجعة المنظّم + تغيير خطة لكل مباراة)
- **تقرير PDF** (جدول البطولة بخط Cairo RTL بدعم 3 لغات: عربي، إنجليزي، كردي)
- **25 Cloud Function** منشورة ومؤكَّدة على `touba-3ds`
- **قواعد Firestore + Storage + الفهارس** منشورة
- **تصميم الواجهة v2** مطبَّق بالكامل بنسبة **100% على كافة الشاشات الـ 60** مع خلفية `DecoratedBackground` وبطاقات منحنية `AppColors.surfaceDark` وحدود متناسقة وتدرجات خضراء متطابقة.
- **ترقية أدوات أندرويد**: ترقية Gradle wrapper إلى `8.14`، AGP إلى `8.11.1`، Kotlin إلى `2.1.10`، و Desugaring إلى `2.1.4`.

### 🔧 ترقية أدوات أندرويد (Android Toolchain) — مُنجزة 100% بالكامل ✅
- Gradle: `8.14` (أحدث إصدار مستقر متوافق).
- Android Gradle Plugin (AGP): `8.11.1`.
- Kotlin: `2.1.10`.
- Desugaring (`desugar_jdk_libs`): `2.1.4`.
- Google Services Plugin: `4.4.2`.

### 🎨 تصميم الواجهة v2 — مُنجز 100% بالكامل ✅
- جميع الشاشات (60 شاشة) مغلفة بـ `DecoratedBackground` مع `Scaffold(backgroundColor: Colors.transparent)`
- نظام البطاقات المظلمة الفاخرة `AppColors.surfaceDark` (0xFF111C24) مع حواف `16px` وحدود دقيقة `0xFF1E2A38`
- الحقول والإدخالات المتناسقة والأزرار المتجاوبة مع الثيمين الفاتح والداكن.

### 🌍 التعريب — مُنجز 100% بالكامل ✅
- **3 لغات متطابقة 100%:** عربية (قالب) · إنجليزية · **كردية سوراني** (ترجمة احترافية)
- **1388 مفتاحاً** متطابقاً في كل ملفات ARB
- `AuthErrorResolver`: ترجمة كاملة لأخطاء المصادقة و Firebase Auth
- المحافظات والمناطق الـ 18: دعم كامل للغات الثلاث في `iraq_regions.dart`، `city_model.dart`، `district_model.dart`، `location_repository.dart`
- شاشات إدارة المناطق والمحافظات: إضافة حقل الاسم بالكردية `nameKu`
- تقارير PDF: متعددة اللغات (AR / EN / KU) مع ضبط اتجاه النص RTL/LTR
- **التغطية: 100% لجميع الشاشات والرسائل والتقارير**

### 📊 مؤشّرات الجودة (فُحص 20 آب 2026 مباشرة)
| الفحص | النتيجة |
|---|---|
| `flutter analyze lib` | ✅ **No issues found (0 errors / 0 warnings)** |
| `flutter test` | ✅ **20/20 — All tests passed** |
| ESLint (functions) | ✅ 0 (مؤكَّد) |

---

## 4) 🔜 ما تبقّى — ما قبل الإطلاق

| # | المهمة | الحالة |
|---|---|---|
| **1** | اختبار على هاتف حقيقي (أندرويد / iOS) | 📱 جاهز للاختبار الآن |
| **2** | تجهيز المتجر (لقطات + وصف + أيقونة + فيديو) | لم يبدأ |
| **3** | استضافة سياسة الخصوصية | لم تبدأ (مطلوبة للمتجر) |

---

## 5) ⚠️ معلّقات ومشاكل معروفة

| البند | الحالة |
|---|---|
| الويب | **لا يعمل** — `firebase_options.dart` بلا فرع `web` (مؤجّل بقرار المالك) |
| ملفات Firebase على الماك | **نقل يدوي** — انظر [`دليل_الإعداد_على_الماك.md`](دليل_الإعداد_على_الماك.md) |
| بناء أندرويد | ✅ يبني ويعمل. `gradle.properties` مضبوط (Kotlin 2G + heap 4G + parallel/caching) |
| الاختبارات | 20 اختباراً تغطي المصادقة والفرق وتوطين المحافظات والترجمة |

---

## 6) أرقام المشروع (مُحقَّقة 19 آب 2026)

| البند | القيمة |
|---|---|
| ملفات Dart في `lib/` | **~148** |
| الشاشات | **60** (في 19 مجلد) |
| النماذج | **21** |
| المستودعات | **13** |
| الكيوبتس | **12** |
| الودجات المشتركة | **13** (في `widgets/core/`) |
| الخدمات | **6** |
| Cloud Functions منشورة | **25** |
| مفاتيح الترجمة | **1353** × 3 لغات |

---

## 7) سير العمل عبر جهازين

**قبل الرفع (أي جهاز):**
```bash
# حدّث الحالة_الحالية.md يدوياً أو بـ /handoff
git add -A && git commit -m "..." && git push
```

**بعد السحب على الجهاز الآخر:**
```bash
git pull
git submodule update --init --recursive   # لجلب craftsman_backup
flutter pub get
flutter gen-l10n                          # إن كانت ملفات arb تغيّرت
```

---

## 8) مستندات مرجعية

| المستند | الغرض |
|---|---|
| [`تقرير_الحالة_الشاملة.md`](تقرير_الحالة_الشاملة.md) | تقرير تفصيلي بالأرقام والنِّسب (مُحدَّث 19 آب) |
| [`تصميم_الأساس.md`](تصميم_الأساس.md) | المخطط الهندسي المعتمد (سكيمة/أدوار/قواعد/فهارس/دوال) |
| [`تحليل_المشروع_الشامل.md`](تحليل_المشروع_الشامل.md) | سجل التنفيذ التاريخي (القسم 19) |
| [`خطة_الوجبة_السابعة.md`](خطة_الوجبة_السابعة.md) | آخر وجبة مُنجزة (كاملة ✅) |
| [`دليل_الإعداد_على_الماك.md`](دليل_الإعداد_على_الماك.md) | تجهيز الماك خطوة بخطوة |
| [`دليل_النشر.md`](دليل_النشر.md) | النشر على Firebase والمتاجر |
| `CLAUDE.md` + `.claude/rules/` | قواعد الجلسة المُلزِمة |
